.row
  = form_tag(cards_path, method: :get, class: 'col s12') do
    .row
      .input-field.col.s12
        = text_field_tag :term, @term
        %label{:for => "term"} Nome da carta
        = submit_tag 'Buscar', class: 'btn waves-effect waves-light'
= render partial: 'card_list', locals: { cards: @cards }
